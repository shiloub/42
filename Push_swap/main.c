#include "lst.h"

int main(int ac, char **av)
{
	//t_intlist	*a;
	//t_intlist *b;
	//t_strlist	*op;
	printf("retour check : %d\n", check_args(ac, av));
	///a = parsing(ac, av);
	
	//op = NULL;
    //b = NULL;
    //printlsts(a, b);
    //printlsts(a, b);
	//sort_pushmin(a, &op);
    //ft_freelst(a);
    //a = parsing(ac, av);
	//print_strlst(op);
    //printf("\n");
	//printlsts(a, b);
	//take_op(&a, &b);
	//ft_freelst(a);
	//ft_freelst(b);
	return 0;
}